# 컨텍스트 요약

## 분석 대상

- 세션 ID: `<sessionId>`
- Org Alias: `<orgAlias>`
- 대상 유형: `<target.type>`
- 대상 컴포넌트: `<target.component>`
- 진입점: `<target.entryPoint>`
- 소스 참조: `<target.sourceRef>`

## 대상 결정 근거

`<analysisSummary.targetResolution>`

## 확인한 파일 및 메타데이터

### 파일

- `<analysisSummary.inspectedFiles>`

### 메타데이터

- `<analysisSummary.inspectedMetadata>`

### Org 컨텍스트

- `<analysisSummary.inspectedOrgContext>`

## 코드 동작 이해

### 확인된 동작

- `<analysisSummary.confirmedBehavior>`

### 추론한 동작

- `<analysisSummary.inferredBehavior>`

## 의존성

| 유형 | 이름 | 이유 |
| --- | --- | --- |
| `<dependency.type>` | `<dependency.name>` | `<dependency.reason>` |

## 부작용 및 연계 동작

- `<analysisSummary.sideEffects>`

## 리스크 및 공백

| 수준 | 설명 | 완화 방안 |
| --- | --- | --- |
| `<risk.level>` | `<risk.description>` | `<risk.mitigation>` |

## 가정

- `<assumptions>`

## 불확실한 점

- `<unknowns>`

## 후속 질문

- `<followUpQuestions>`
