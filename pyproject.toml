[project]
name = "salesforce-agent-framework"
version = "0.1.0"
description = "CLI-first Salesforce Test Agent MVP framework"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.0",
]

[project.scripts]
sf-test-agent = "salesforce_agent_framework.cli:main"

[tool.setuptools.packages.find]
include = ["salesforce_agent_framework*"]
